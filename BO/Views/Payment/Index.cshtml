@model BO.ViewModels.PaymentListViewModel

@{
    ViewData["Title"] = "Payments";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Payment</a>
</p>

@await Html.PartialAsync("_FlashMessages")

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Payments[0].Name)</th>
            <th>@Html.DisplayNameFor(m => m.Payments[0].PaymentTypeName)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var p in Model.Payments)
        {
            <tr>
                <td>@p.Name</td>
                <td>@p.PaymentTypeName</td>
                <td class="text-end">
                    <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-info btn-sm">Details</a>
                    <button type="button"
                            class="btn btn-danger btn-sm"
                            data-bs-toggle="modal"
                            data-bs-target="#deleteSimpleModal"
                            data-entity-id="@p.Id"
                            data-entity-value="@p.Name">
                        Delete
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>

<br />
<br />
<br />

<div class="mt-3">
    @if (Model.HasPreviousPage)
    {
        <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex - 1)" class="btn btn-outline-primary">Previous</a>
    }
    Page @Model.PageIndex of @Model.TotalPages
    @if (Model.HasNextPage)
    {
        <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex + 1)" class="btn btn-outline-primary">Next</a>
    }
</div>

<!-- Include reusable delete modal -->
@await Html.PartialAsync("_DeleteSimpleModal", new BO.ViewModels.DeleteModalViewModel
{
    EntityName = "Payment",
    EntityValue = "",
    EntityId = 0,
    ActionUrl = Url.Action("Delete", "Payment")
})
